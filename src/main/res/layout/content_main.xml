<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.darkknight.mahinekahisab.MainActivity"
    tools:showIn="@layout/activity_main">

    <TextView
        android:id="@+id/file_name"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@+id/browse"
        android:text="No file selected..." />

    <Button
        android:id="@+id/browse"
        android:layout_width="96dp"
        android:layout_height="wrap_content"
        android:text="Browse"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:onClick="browseFile"/>

    <TextView
        android:id="@+id/month_name"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:layout_below="@+id/file_name"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@+id/browse"
        android:text="Select the Mahina"
        android:fontFamily="sans-serif"
        />

    <Spinner
        android:id="@+id/month"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/browse"
        />

    <Button
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/month_name"
        android:text="Hisab kariye"
        android:onClick="doHisab"/>
</RelativeLayout>
